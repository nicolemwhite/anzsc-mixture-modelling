model
{
for( i in 1 : N ) {
y[i] ~ dnorm(mu[i], tau.y[i])
mu[i] <- theta[z[i]]
tau.y[i] <- tau[z[i]]
z[i] ~ dcat(eta[])
}	
eta[1:K] ~ ddirich(alpha[])

for (k in 1:K){
theta[k] ~ dnorm(b0,omega[k])
omega[k] <- N0*tau[k]
tau[k] ~ dgamma(c0, C0) 
sigma[k] <- 1 / sqrt(tau[k])
}

}